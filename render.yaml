services:
  - type: web
    name: rag-production
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python api.py
    healthCheckPath: /health
    envVars:
      - key: GROQ_API_KEY
        sync: false
      - key: PYTHON_VERSION
        value: 3.11.0
